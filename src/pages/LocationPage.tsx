

const LocationPage = (): JSX.Element => {

  return (
      <div style={{backgroundColor: `black`, color: `white`, fontSize: `20px`, height: `100vh`}}>

          There will be some location in the future!!!
      </div>
  );
};

export default LocationPage;
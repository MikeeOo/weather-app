import styled from "styled-components";

export const LocationDescStyled = styled.div`
  border-top: 1px solid ${({theme}) => theme.color.pseudoElement};
  font-size: 1.5rem;
  display: flex;
  align-items: center;
  height: 4.2rem;
`;
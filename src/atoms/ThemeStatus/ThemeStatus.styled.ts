import styled from "styled-components";
import {flexBox} from "../../styles/mixins";

export const ThemeStatusStyled = styled.div`
       ${flexBox({jc: "space-between", ai: "center"})};
       width: 7.6rem;
`;
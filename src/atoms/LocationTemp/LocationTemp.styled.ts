import styled from "styled-components";

export const LocationTempStyled = styled.div`
  border-bottom: 1px solid ${({theme}) => theme.color.pseudoElement};

  font-size: 3.1rem;
  display: flex;
  align-items: center;
  height: 4.2rem;
`;